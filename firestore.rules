rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Backend API handles all data access via Firebase Admin SDK.
    // Deny direct unauthenticated client SDK reads and writes by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
